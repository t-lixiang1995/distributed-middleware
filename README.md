# 前言

distributed-middleware(分布式中间件学习)

目前结构如下：

```
[-] xxx
  ├──[-] zookeeperDemo              // zookeeper简单的实现。
  ├──[-] shardingJdbcDemo           // Sharding-JDBC的简单应用实例。
  ├──[-] rabbitmqDemo               // RabbitMQ应用实例
  |——[-] mongodbTest                // mongodb的简单应用实例
  |——[-] kafka应用实例                                        // kafka的简单应用实例
  |——[-] hystrixDemo                // hystrix应用实例
  |——[-] dubbo_demo                 // Dubbo应用实例
```
